#include <bits/stdc++.h>

using namespace std;

int n , m , q;
char v[1007][1007];

int main() {
	ios_base::sync_with_stdio(0); cin.tie(NULL);
	int n ; cin >> n >> m >> q;
	for (int i = 1 ; i <= n ; i++) {
		for (int j = 1 ; j <= m ; j++) {
			cin >> v[i][j];
		}
	}
	for (int i = 1 ; i <= n ; i++) {
		
	}


	return 0;
}