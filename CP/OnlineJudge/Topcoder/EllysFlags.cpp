#include <bits/stdc++.h>

using namespace std;

int n , m;
int f[11][11][1048576];

int main() {
    ios::sync_with_stdio(0); cin.tie(NULL);
    cout.tie(NULL);
    cin >> n >> m;
    for (int i = 1 ; i <= n ;i ++) {
        for (int j = 1 ; j <= m ; j++) {
            for (int mask = 0 ; mask < (1 << (2*m)) ; mask++) {
                
            }
        }
    }
    return 0;
}